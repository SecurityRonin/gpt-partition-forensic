//! CRC-32/ISO-HDLC (the zlib/PNG/GPT CRC) — stub, implemented in the GREEN step.

/// Compute the CRC-32 of `data`. (Stub — returns 0 until implemented.)
#[must_use]
pub fn checksum(data: &[u8]) -> u32 {
    let _ = data;
    0
}
